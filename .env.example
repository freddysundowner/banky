# ═══════════════════════════════════════════════════════════════════
#  BANKY - Bank & Sacco Management System
#  Environment Configuration
# ═══════════════════════════════════════════════════════════════════
#
#  Copy this file to .env and fill in your values:
#    cp .env.example .env
#    nano .env
#
# ═══════════════════════════════════════════════════════════════════


# ── REQUIRED ─────────────────────────────────────────────────────

# PostgreSQL connection string
# Local:  postgresql://banky:your_password@localhost:5432/banky
# Remote: postgresql://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://banky:your_password@localhost:5432/banky

# Deployment mode: "enterprise" for self-hosted
DEPLOYMENT_MODE=enterprise

# License key (Direct/Enterprise purchases only)
# Provided after purchase. Controls edition limits (members, staff, branches).
# Format: BANKY-{EDITION}-{YEAR}-{UNIQUE_ID}
# Editions: BAS (Basic), STD (Standard), PRE (Premium), ENT (Enterprise)
# CodeCanyon buyers: leave blank -- full version is unlocked automatically
LICENSE_KEY=

# Random secret for session encryption (minimum 32 characters)
# Generate with: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-random-string-at-least-32-characters

# Application port (default: 5000)
PORT=5000


# ── M-PESA INTEGRATION (Optional) ───────────────────────────────
# Get credentials from https://developer.safaricom.co.ke

# MPESA_CONSUMER_KEY=your_consumer_key
# MPESA_CONSUMER_SECRET=your_consumer_secret
# MPESA_SHORTCODE=your_paybill_or_till
# MPESA_PASSKEY=your_lipa_na_mpesa_passkey
# MPESA_CALLBACK_URL=https://yoursite.com/api/mpesa/callback
# MPESA_ENVIRONMENT=sandbox


# ── EMAIL (Optional) ────────────────────────────────────────────
# Brevo (formerly Sendinblue) for transactional emails
# Get API key from https://app.brevo.com

# BREVO_API_KEY=your_brevo_api_key
# FROM_EMAIL=noreply@yourorganization.co.ke
# FROM_NAME=Your Organization


# ── SMS GATEWAY (Optional) ──────────────────────────────────────
# SMS is configured from Settings within the BANKY dashboard.
# You can also set defaults here:

# SMS_API_KEY=your_sms_api_key
# SMS_SENDER_ID=BANKY


# ── ADVANCED (Usually not needed) ───────────────────────────────

# Node environment
# NODE_ENV=production

# Stripe (for subscription payments)
# STRIPE_SECRET_KEY=
# STRIPE_PUBLISHABLE_KEY=

# Paystack (for subscription payments)
# PAYSTACK_SECRET_KEY=
# PAYSTACK_PUBLIC_KEY=
